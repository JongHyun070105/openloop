# OpenLoop Mobile

Flutter client for the OpenLoop AI Action Calendar.

The current foundation includes the Loop overview, Appointment and Deadline cards, the capture entry sheet, and the core privacy message. Native share extensions, calendar access, notifications, and API integration are the next implementation boundaries.

```bash
flutter pub get
flutter run
```

Validation:

```bash
flutter analyze
flutter test
flutter build web
```
